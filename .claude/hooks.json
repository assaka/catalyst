{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash.*git push",
        "hooks": [
          {
            "type": "command",
            "command": "echo '🚀 Deployment triggered - checking logs in 30 seconds...' && sleep 30 && render logs --tail 50 --service daino-backend-fzhu | grep -i error"
          }
        ]
      }
    ],
    "Stop": [
      {
        "type": "command", 
        "command": "echo '✅ Session ended - run: render logs --tail 20 --service daino-backend-fzhu to check final deployment status'"
      }
    ]
  }
}